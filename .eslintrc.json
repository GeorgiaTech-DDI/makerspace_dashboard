{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["process.env"],
            "message": "Please import from lib/config instead of using process.env directly."
          }
        ]
      }
    ]
  }
}
